name: Keepalive
on:
  schedule:
    - cron: '0 */2 * * *'  # cada 2 horas
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping F1-Explained
        run: curl -I https://f1-explained.streamlit.app/
